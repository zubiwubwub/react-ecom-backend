import { jsx, Stack } from '@keystone-ui/core';
import 'react';
import '@babel/runtime/helpers/defineProperty';
import '@keystone-ui/button';
import { AlertTriangleIcon } from '@keystone-ui/icons/icons/AlertTriangleIcon';
import { a as ErrorContainer } from '../../../dist/Errors-34998490.esm.js';
import '@apollo/client';
import { S as SignoutButton } from '../../../dist/SignoutButton-dbfda3aa.esm.js';

/* @jsx jsx */
const NoAccessPage = ({
  sessionsEnabled
}) => {
  return jsx(ErrorContainer, null, jsx(Stack, {
    align: "center",
    gap: "medium"
  }, jsx(AlertTriangleIcon, {
    size: "large"
  }), jsx("div", null, "You don't have access to this page."), sessionsEnabled ? jsx(SignoutButton, null) : null));
};

export { NoAccessPage };
