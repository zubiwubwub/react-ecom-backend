import type { KeystoneSystem, KeystoneConfig } from '@keystone-next/types';
declare type AppTemplateOptions = {
    configFileExists: boolean;
    projectAdminPath: string;
};
export declare const appTemplate: (config: KeystoneConfig, system: KeystoneSystem, { configFileExists, projectAdminPath }: AppTemplateOptions) => string;
export {};
