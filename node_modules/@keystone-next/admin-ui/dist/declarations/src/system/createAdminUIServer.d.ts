import express from 'express';
import type { KeystoneSystem, KeystoneConfig, SessionImplementation } from '@keystone-next/types';
export declare const createAdminUIServer: (ui: KeystoneConfig['ui'], system: KeystoneSystem, dev: boolean, sessionImplementation?: SessionImplementation | undefined) => Promise<(req: express.Request, res: express.Response) => Promise<void>>;
